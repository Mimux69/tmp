version: "3.8"

services:
  nginx:
    build: requirements/nginx/.
    container_name: nginx
    ports:
      - "8080:80"
    volumes:
      - ./web:/var/www/html
    # networks:
    #   - inception_network

  wordpress:
    build: requirements/wordpress/.
    container_name: wordpress
    volumes:
      - ./web:/var/www/html
    # networks:
    #   - inception_network
    # expose:
    #   - "9000"

# networks:
#   inception_network:
#     driver: bridge

volumes:
  
